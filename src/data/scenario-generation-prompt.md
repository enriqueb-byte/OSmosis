# Prompt used to generate scenarios (e.g. in Gemini)

Paste or describe the prompt you use to generate scenarios for `scenarios.json`. That way you can reproduce or tweak the style when adding more scenarios.

<!-- Example:
You are generating practice scenarios for Jobber (field service management software) support and sales training. Each scenario should be a short customer-style question that a rep might need to answer. Output JSON with: id, pillar (1–8), tier (connect|grow), difficulty (1–3), persona (busy_bee|micro_manager|burned_pro|tech_rookie), query. ...
-->
